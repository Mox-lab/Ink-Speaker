# 2026-07-16

## Flyway 迁移脚本整合

- 将原 V1/V2/V8/V9/V10/V11 共 6 个迁移文件合并为单一 `V1__init_schema.sql`
- 删除了旧的 V2、V8、V9、V10、V11 迁移文件
- 合并时修复了 V8 (novel_chapter_history) 和 V9 (agent_log) 缺少 `updated_at` 列的问题
- 保留了 sequence 修复和种子数据
- 文件头包含可选的 DROP TABLE 段（默认注释），方便删库重建

## MyBatis-Plus 问题修复（之前）

- 将 `mybatis-plus-spring-boot3-starter` 升级为 `mybatis-plus-spring-boot4-starter` 3.5.16
- 将 `mybatis-plus` 配置从 `spring:` 嵌套下移到顶层
